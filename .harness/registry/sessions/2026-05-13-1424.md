# Session 2026-05-13 14:24

## 完成

- **feat-104** 排队发送 UI 仿真 — 纯前端队列管理，输入框 streaming 时不禁用，按钮变为"排队 ⏸"，底部显示排队内容+取消按钮，进程退出后自动发送

## 改动文件

- `src/pages/ChatPage.tsx` — 新增 `queuedText` 状态、`handleQueueMessage`/`handleCancelQueue` 回调、auto-send useEffect、会话切换清理
- `src/components/ChatView.tsx` — Props 新增 queue 相关属性，submit() 区分 send/queue 模式，textarea 移除 disabled，按钮改为"排队 ⏸"，新增 queue-indicator UI
- `src/index.css` — 新增 `.queue-indicator` / `.queue-label` / `.queue-text` / `.queue-cancel` 样式
- `.harness/state/features.json` — feat-104 passes: true；修复 JSON 中中文引号编码问题（ASCII " → Unicode ""）

## 未完成

- Sprint 1 剩余：feat-110 首次使用引导
- Sprint 2 全部待做：feat-105～109

## 下次从哪开始

feat-110 首次使用引导（hermes 安装检测 + 三步引导页）
