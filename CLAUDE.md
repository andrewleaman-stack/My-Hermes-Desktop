# Hermes Desktop — Agent Rules

See `AGENTS.md`. This repository expects agents to preserve the process-based Hermes CLI model, avoid unnecessary refactors, and verify frontend and Rust changes before handing work back.
